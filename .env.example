# Supabase (memoire bulletin board). Copy to .env.local and fill in.
# The anon key is a PUBLIC key; Row-Level Security protects the table.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
